apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: example-a-record
spec:
  endpoints:
    - dnsName: crds.dnstest.pinax.network
      recordType: A
      targets:
        - 127.0.0.1
